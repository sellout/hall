let DefaultType = ./DefaultType

let Self = ./Type

let default = ./default

let schema =
      λ(a : Type) →
          { Type = Self a, default = default a }
        : { Type : Type, default : DefaultType a }

let testMinimalRequiredFields = (schema <>)::{=}

in  schema
